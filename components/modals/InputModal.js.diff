--- a/components/modals/InputModal.js
+++ b/components/modals/InputModal.js
@@
-export default function InputModal({ onClose, onSuccess }) {
+export default function InputModal({ onClose, onSuccess, fullscreen = false }) {
@@
-  return (
+  return (
+    fullscreen ? <div style={{ minHeight: '100vh', background: 'var(--bg)', paddingBottom: 90 }}>
+      <div style={{ padding: '16px', fontSize: 18, fontWeight: 700 }}>Input Transaksi</div>
+      {/* gunakan existing AI + manual input layout */}
+      {/* tambahkan panel riwayat 10 transaksi terakhir di atas form */}
+      {/* tampilkan timer recording + waveform saat isRecording */}
+      {/* tampilkan scan overlay animation saat imagePreview */}
+      {/* tampilkan processing animation saat aiLoading */}
+    </div> :
