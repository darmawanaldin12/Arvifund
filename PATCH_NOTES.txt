PETUNJUK PATCH components/modals/InputModal.js
==============================================

Diff dari zip kamu sudah benar. Terapkan perubahan ini ke InputModal.js:

1. Ubah signature fungsi:
   SEBELUM:  export default function InputModal({ onClose, onSuccess }) {
   SESUDAH:  export default function InputModal({ onClose, onSuccess, fullscreen = false }) {

2. Tambahkan wrapper fullscreen di return statement:
   SEBELUM:
     return (
       <existing content>
     )

   SESUDAH:
     return (
       fullscreen ? (
         <div style={{ minHeight: '100vh', background: 'var(--bg)', paddingBottom: 90 }}>
           <div style={{ padding: '16px', fontSize: 18, fontWeight: 700 }}>Input Transaksi</div>
           {/* existing AI + manual input layout */}
           {/* panel riwayat 10 transaksi terakhir di atas form */}
           {/* timer recording + waveform saat isRecording */}
           {/* scan overlay animation saat imagePreview */}
           {/* processing animation saat aiLoading */}
         </div>
       ) : (
         <existing modal content>
       )
     )

CATATAN PENTING:
- File ini TIDAK perlu diubah untuk fix error build.
- Error build disebabkan oleh app/input/page.js yang tidak punya DataProvider.
- File app/input/page.js sudah diperbaiki di folder ini.
